version: "3.8"

services:
  kali:
    build: .
    container_name: kali-pentest
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Guayaquil
      - USER=kali
      - PASSWORD=Vps2026
      - SUDO_ACCESS=true
    volumes:
      - ./config:/config
      - ./data:/data
    ports:
      - "8080:8080"
      - "4444:4444"
    restart: unless-stopped